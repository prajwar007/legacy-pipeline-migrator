source_fields:
  - transaction_id
  - account_id
  - txn_type
  - amount
  - txn_date
target_table: transactions
validation:
  transaction_id_regex: "^TXN-[0-9]{6}$"
  account_id_regex: "^ACCT-[0-9]{6}$"
  amount_regex: "^[0-9]+(\\.[0-9]{1,2})?$"
  valid_txn_types:
    - CREDIT
    - DEBIT

